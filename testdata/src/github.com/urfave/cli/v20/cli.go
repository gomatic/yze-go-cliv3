// Package cli is a stub of a hypothetical urfave/cli v20 for analysistest
// fixtures: it shares the "github.com/urfave/cli/v2" string prefix without
// being the v2 module or one of its subpackages, so a naive prefix match would
// wrongly flag it.
package cli

// App is a stand-in for a hypothetical v20 App type.
type App struct{ Name string }
