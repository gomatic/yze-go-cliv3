// Package cli is a minimal stub of github.com/codegangsta/cli for analysistest
// fixtures: the name urfave/cli was renamed FROM, still served by GitHub. At
// v1.20.0 its tree is byte-identical to github.com/urfave/cli@v1.20.0.
package cli

// App is a stand-in for the v1 App type.
type App struct{ Name string }
